# Spring 配置
spring:
  profiles:
    active: docker
    include: druid
  # 数据源配置
  datasource:
    druid:
      master:
        url: jdbc:mysql://db:3306/ry?useUnicode=true&characterEncoding=utf8&zeroDateTimeBehavior=convertToNull&useSSL=true&serverTimezone=GMT%2B8
        username: root
        password: =@ZxCV1N
  
  # Redis 配置
  data:
    redis:
      host: redis
      port: 6379
      password: 
      database: 0

# 若依配置
ruoyi:
  # 文件路径 示例（ Windows配置D:/ruoyi/uploadPath，Linux配置 /home/ruoyi/uploadPath）
  profile: /home/ruoyi/uploadPath
